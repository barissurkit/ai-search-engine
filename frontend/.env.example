# Leave empty in local development to use the Vite /api proxy.
# For deployed environments, set this to the public backend origin, e.g. https://api.example.com.
# Do not put backend secrets or provider API keys in this frontend environment file.
VITE_API_BASE_URL=
